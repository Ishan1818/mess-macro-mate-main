const fs=require('fs');const {execSync}=require('child_process');const filePath=process.argv[2];if(!filePath)throw new Error('Need file path');const s=fs.readFileSync(filePath,'utf8');let lo=0,hi=s.length,ans=-1;while(lo<=hi){let mid=Math.floor((lo+hi)/2);const chunk=s.slice(0,s.length-mid);fs.writeFileSync('tmp_chunk.js',chunk);try{execSync('node --check tmp_chunk.js',{stdio:'pipe'});ans=mid;hi=mid-1;}catch(e){lo=mid+1;}}if(ans===-1)console.log('File still invalid after removing whole content');else{console.log('Remove at least',ans,'trailing chars to make file pass syntax check');const idx=s.length-ans;const lines=s.slice(0,idx).split(/\r?\n/);const ln=lines.length;const col=lines[lines.length-1].length+1;console.log('Context at',idx,'line',ln,'col',col);console.log('---prev 10 lines---');console.log(lines.slice(Math.max(0,ln-11),ln).join('\n'));}
try{fs.unlinkSync('tmp_chunk.js');}catch(e){}